const serverPort = 3000

const successStatus = "success"

// Novel
const newestNovelsSize = 30
const popularNovelsSize = 50

module.exports = {successStatus, serverPort, newestNovelsSize, popularNovelsSize}
